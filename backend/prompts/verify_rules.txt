Verify this parsed order against restaurant rules:

Parsed: {parsed_order}
Errors: {validation_errors}

CRITICAL RULES:
1. table_num MUST exist and be 1-20 → REJECT if missing
2. qty must be integer >0
3. price must match: Idly=32, Dosa=110, Vada=30, Coffee=25

Output ONLY:
{{"action": "approve|reject", "reason": "explanation"}}
